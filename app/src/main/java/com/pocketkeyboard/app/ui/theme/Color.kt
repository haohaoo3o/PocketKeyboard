package com.pocketkeyboard.app.ui.theme

import androidx.compose.ui.graphics.Color

/** 纯黑，OLED 省电背景。 */
val PureBlack = Color(0xFF000000)

/** 纯白，主前景色。 */
val PureWhite = Color(0xFFFFFFFF)

/** 极简灰，仅用于极轻的分隔与禁用态。 */
val MinimalGray = Color(0xFF1A1A1A)
